name: tes
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  intl: ^0.20.2
  google_fonts: ^6.2.1
  timeago: ^3.6.1

  # --- Paket Baru untuk Arsitektur & Fitur ---
  flutter_bloc: ^8.1.6
  get_it: ^7.7.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  equatable: ^2.0.5 # Berguna untuk BLoC
  responsive_sizer: ^3.3.1
  go_router: ^14.2.0
  flutter_animate: ^4.5.0
  lottie: ^3.1.2
  freezed_annotation: ^2.4.1

  # --- Library dari Tugas ---
  getwidget: ^4.0.0
  device_info_plus: ^10.1.0
  webview_flutter: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo/
    - assets/kamar_kost/
